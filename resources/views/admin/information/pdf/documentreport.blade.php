@extends('layouts.newReport')

@section('title', $title)

@section('content')

<div class = "rep">
        <br>
        <br>
        <b> {{ $title }} </b>
        </h6>
        <br>
        <b>Generated By:</b><i>: {{ Auth::user()->first_name }} {{ Auth::user()->last_name }} (#{{ Auth::id() }})</i>
        <br>
        <b>Position:</b> <i>{{ Auth::user()->user_role->role }} </i>
        <br>
        <b>Date Generated:</b><i>{{ date('Y-m-d H:i:s') }}</i>
    </div>

    <hr style="width:45%; margin-left: 0.1rem; background-color:gray; height: 2px;" >
    <div class = "timef">
        <h6>
        <b>Timeframe</b>
    </div>

    <div class ="dits">
        <div class="row p-4">
            <div class="column" style="font-size: 18px">
                <p class="font-weight-bold">Date Start:</p>
                <p class="font-weight-bold">Date End:</p>
                <p class="font-weight-bold">Sort by:</p>
            </div>
            <div class="column" style="font-size: 18px">
                <p class="text-right"> {{$date_start}} </p>
                <p class="text-right"> {{ $date_end  }} </p>
                <p class="text-right">  {{ str_replace(array("documents."),"",$sort_column ); }} ({{{ $sort_option }}}) </p>
            </div>
            <hr style="width:100%; background-color:gray; height: 2px;" >
        </div>
    </div>
<div class="container mb-3">
    <h4 class="text-center">Overall Statistics</h4>
        <hr/>
        <div class="row">
            <div class="col-sm">
                <div class="row">
                    <div class="col-sm-6">
                        Document publish this day:
                    </div>
                    <div class="col-sm-6">
                        <div class="font-weight-bold">{{ $documentsData->this_day_count }}</div>
                    </div>

                    <div class="col-sm-6">
                        Document publish this month:
                    </div>
                    <div class="col-sm-6">
                        <div class="font-weight-bold">{{ $documentsData->this_month_count }}</div>
                    </div>
                    <div class="col-sm-6">
                         Document publish this year:
                    </div>
                    <div class="col-sm-6">
                        <div class="font-weight-bold">{{ $documentsData->this_year_count }}</div>
                    </div>
                </div>
            </div>
</div>
    <div class="regiz">
        <div class="regi">
            <pre> <b>{{ $modelName }} count:</b> {{ $documents->count() }}</pre>
        </div>
    </div>

    
    <table class="tableContent mb-4">
        <thead style="background: #F5F5F5;">
            <tr>
                    <th>ID</th>
                    <th>Description</th>
                    <th>Type</th>
                    <th>Year</th>
                    <th>PDF</th>
                    <th>Created At</th>
                    <th>Updated At</th>
            </tr>
        </thead>
        <tbody>
            @foreach ($documents as $document)
                <tr>
                <td>{{ $document->id }}</td>
                <td>{{ $document->description }}</td>
                @if ($document->custom_type)
                        <td>{{ $document->custom_type }}</td>
                    @else
                        <td>{{ $document->type->name }}</td>
                    @endif
                    <td>{{ $document->year}}</td>
                    <td>{{ $document->pdf_name}}</td>
                    <td>{{ $document->created_at }}</td>
                    <td>{{ $document->updated_at }}</td>
                </tr>
            @endforeach
        </tbody>
    </table>

@endsection